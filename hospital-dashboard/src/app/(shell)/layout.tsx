import { OncoCareLayout } from "@/components/oncocare/OncoCareLayout";

export default function ShellLayout({ children }: { children: React.ReactNode }) {
  return <OncoCareLayout>{children}</OncoCareLayout>;
}
